/**
 * FoodLens Services — API Client (legacy)
 * Replaced by apiClient.ts (axios-based with auth interceptor).
 * Kept for backward compatibility — re-exports from apiClient.
 */

export {default as apiClient, API_BASE_URL} from './apiClient';
